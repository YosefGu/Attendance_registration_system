import { Text, View, Button } from "react-native"
import styles from "../../utils/globalStyles"

export const ManagedTeam = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>ManagedTeam</Text>
    </View>
  )
}
